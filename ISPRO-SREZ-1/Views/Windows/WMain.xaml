<Window x:Class="SREZ_1.WMain"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SREZ_1"
        mc:Ignorable="d"
        Loaded="Window_Loaded"
        WindowStartupLocation="CenterScreen"
        Title="{Binding ElementName=FrMain, Path=Content.Title, FallbackValue=Главное окно}" 
        MinHeight="450" MinWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition Width="auto"/>
            </Grid.ColumnDefinitions>
            
            <StackPanel Orientation="Horizontal">
                <Image Height="50" Source="{Binding User.Photo}"/>
                <Label Content="{Binding User.FullName}"/>
                <Label Content="{Binding User.GetTypeUser}"/>
            </StackPanel>

            <Button x:Name="BtnLogOut" Grid.Column="1" Click="BtnLogOut_Click">Выйти</Button>
        </Grid> 
        
        <Frame Grid.Row="1" x:Name="FrMain" NavigationUIVisibility="Hidden"/>
    </Grid>
</Window>